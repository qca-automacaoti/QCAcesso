/**
 * Serviço de Autenticação (auth.service.ts)
 * Descrição: Executa a lógica de autenticação de usuários, validação de senhas
 * criptografadas (hash), geração de JWT tokens e controle de sessão do sistema.
 */
