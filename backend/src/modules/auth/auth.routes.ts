/**
 * Rotas de Autenticação (auth.routes.ts)
 * Descrição: Mapeia as rotas HTTP do módulo de autenticação (ex.: POST /auth/login)
 * associando cada endpoint aos métodos do respectivo controller.
 */
