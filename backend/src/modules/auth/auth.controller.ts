/**
 * Controller de Autenticação (auth.controller.ts)
 * Descrição: Recebe e trata as requisições HTTP relacionadas à autenticação
 * de usuários, como login no sistema, logout e atualização de tokens de sessão.
 */
