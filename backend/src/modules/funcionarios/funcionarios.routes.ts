/**
 * Rotas de Funcionários (funcionarios.routes.ts)
 * Descrição: Define as rotas HTTP para operações no cadastro de colaboradores
 * (ex: GET /funcionarios, POST /funcionarios, PUT /funcionarios/:id).
 */
