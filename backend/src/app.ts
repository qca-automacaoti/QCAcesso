/**
 * Servidor Principal da Aplicação (app.ts)
 * Descrição: Arquivo principal de inicialização do backend.
 * Configura middlewares globais (CORS, body parser, autenticação),
 * registra as rotas da API e inicializa o servidor HTTP.
 */
