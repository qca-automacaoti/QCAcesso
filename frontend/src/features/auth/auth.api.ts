/**
 * API de Autenticação (auth.api.ts)
 * Descrição: Métodos de requisição HTTP para autenticação de usuários,
 * envio de credenciais (POST /auth/login), atualização de token e encerramento de sessão.
 */
